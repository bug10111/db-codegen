<Window x:Class="DbCodeGen.App.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置"
        Width="640"
        Height="560"
        MinWidth="560"
        MinHeight="480"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" MinHeight="140" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 工作区根默认：目录输入框加浏览按钮，批量代码生成绝对输出路径的根前缀 -->
        <GroupBox Grid.Row="0" Header="工作区根默认" Padding="8" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0"
                         Text="{Binding WorkspaceRoot, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center" />
                <Button Grid.Column="1"
                        Content="浏览..."
                        Margin="8,0,0,0"
                        Padding="14,4"
                        Command="{Binding BrowseWorkspaceRootCommand}" />
            </Grid>
        </GroupBox>

        <!-- LLM 配置：接口地址、API Key（密码框）与模型名；API Key 留空保持原密文 -->
        <GroupBox Grid.Row="1" Header="LLM 配置" Padding="8" Margin="0,0,0,12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="110" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="接口地址" VerticalAlignment="Center" />
                <TextBox Grid.Row="0" Grid.Column="1"
                         Text="{Binding LlmBaseUrl, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,0,6"
                         VerticalContentAlignment="Center" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="API Key" VerticalAlignment="Center" />
                <PasswordBox x:Name="ApiKeyPasswordBox"
                             Grid.Row="1" Grid.Column="1"
                             Margin="0,0,0,4"
                             VerticalContentAlignment="Center" />

                <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                           Text="API Key 留空表示保持原密钥，输入新值将在保存时重新加密覆盖。"
                           FontSize="11"
                           Foreground="Gray"
                           Margin="0,0,0,6" />

                <TextBlock Grid.Row="3" Grid.Column="0" Text="模型" VerticalAlignment="Center" />
                <TextBox Grid.Row="3" Grid.Column="1"
                         Text="{Binding LlmModel, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center" />
            </Grid>
        </GroupBox>

        <!-- 模板搜索目录：目录列表加添加/移除按钮，供模板包管理扫描用户级模板包 -->
        <GroupBox Grid.Row="2" Header="模板搜索目录" Padding="8" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ListBox Grid.Column="0"
                         ItemsSource="{Binding TemplateDirectories}"
                         SelectedItem="{Binding SelectedTemplateDirectory}"
                         Margin="0,0,8,0" />
                <StackPanel Grid.Column="1">
                    <Button Content="添加..."
                            Margin="0,0,0,8"
                            Padding="14,4"
                            Command="{Binding AddTemplateDirectoryCommand}" />
                    <Button Content="移除"
                            Padding="14,4"
                            Command="{Binding RemoveTemplateDirectoryCommand}" />
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- 最近相对输出根：单值记忆上次语义，批量代码生成相对输出根默认值 -->
        <GroupBox Grid.Row="3" Header="最近相对输出根" Padding="8" Margin="0,0,0,12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBox Grid.Row="0"
                         Text="{Binding LastRelativeOutputRoot, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center" />
                <TextBlock Grid.Row="1"
                           Text="相对路径，如 src/main/java；批量代码生成完成后会自动回写最新值。"
                           FontSize="11"
                           Foreground="Gray"
                           Margin="0,4,0,0" />
            </Grid>
        </GroupBox>

        <!-- 底部操作按钮：左侧类型映射入口，右侧保存触发校验落盘、取消直接关闭不保存 -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0"
                    Content="类型映射…"
                    Padding="14,6"
                    HorizontalAlignment="Left"
                    Click="OnOpenTypeMappingClick" />
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="保存"
                        Padding="24,6"
                        Margin="0,0,8,0"
                        IsDefault="True"
                        Command="{Binding SaveCommand}" />
                <Button Content="取消"
                        Padding="24,6"
                        IsCancel="True"
                        Click="OnCancelClick" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
