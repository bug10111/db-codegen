package {{package.basePackage}}.service.impl.{{table.variableName}};

import com.baomidou.mybatisplus.core.metadata.IPage;
import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import {{package.basePackage}}.entity.{{table.variableName}}.{{table.className}};
import {{package.basePackage}}.mapper.{{table.variableName}}.{{table.className}}Mapper;
import {{package.basePackage}}.service.{{table.variableName}}.{{table.className}}Service;
import org.springframework.stereotype.Service;

/**
 * {{table.comment}}服务实现
 */
@Service
public class {{table.className}}ServiceImpl extends ServiceImpl<{{table.className}}Mapper, {{table.className}}> implements {{table.className}}Service {

    /**
     * 分页查询{{table.comment}}
     */
    @Override
    public IPage<{{table.className}}> findPage(Page<{{table.className}}> page) {
        return page(page);
    }

    /**
     * 查询单条{{table.comment}}数据
     */
    @Override
    public {{table.className}} findById(String id) {
        return getById(id);
    }

    /**
     * 新增{{table.comment}}
     */
    @Override
    public void save({{table.className}} entity) {
        super.save(entity);
    }

    /**
     * 根据主键修改{{table.comment}}
     */
    @Override
    public void updateById({{table.className}} entity) {
        super.updateById(entity);
    }

    /**
     * 删除{{table.comment}}
     */
    @Override
    public void deleteByIds(java.util.List<String> ids) {
        removeByIds(ids);
    }
}
